---
- name: Ensure git installed
  package:
    name: git
    state: latest

- name: Setup git config
  copy:
    src: gitconfig
    dest: ~/.gitconfig
    owner: usmon
    group: usmon
    mode: 0700